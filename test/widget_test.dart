import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Dummy test to pass CI/CD', (WidgetTester tester) async {
    // Sebagai placeholder agar flutter test tidak gagal
    // Hapus atau ganti test ini jika ingin mulai menulis unit test secara nyata
    expect(true, isTrue);
  });
}
